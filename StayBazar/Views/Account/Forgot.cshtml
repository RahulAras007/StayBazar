@model StayBazar.Models.ForgotPasswordModel
@using CaptchaMvc.HtmlHelpers
@{

}
@*<div class="container bdy_min_height">
            <div class="row">
                <div class="col-md-12">
                    <h2>Reset your password</h2>
                </div>
            </div>
            @if (Model.Status)
            {
                <div class="row height80"></div>
                <div class="row topmargin12">
                    <div class="col-md-6 col-md-offset-3">
                        <div class="alert alert-success">
                            Password is reset. You will get the new password in your email.
                        </div>
                    </div>
                </div>
            }
            else
            {
                if (Model.ErrorMsg != null && Model.ErrorMsg != "")
                {
                    <div class="row topmargin12">
                        <div class="col-md-6 col-md-offset-3">
                            <div class="alert alert-danger">
                                @Model.ErrorMsg
                            </div>
                        </div>
                    </div>
                }
                else
                { <div class="row height10"></div> }

                <div class="row">
                    <div class="col-md-10 margin10px">
                        @using (Html.BeginForm("Reset", "Account"))
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true);
                            <div>Please enter your email to reset the password</div>
                            <div>@Html.TextBoxFor(m => m.Email) @Html.ValidationMessageFor(m => m.Email)</div>
                            <div>@Html.Captcha("Try another", "Enter the text you see above:", 5, "is required", true)</div>
                            <div class="height10"></div>
                            <div><p><input type="submit" class="btn btn-default" value="Reset" /></p></div>
                        }
                    </div>
                </div>
            }
        </div>
    </div>*@
<div class="bg-color-pw">
    <div class="container">

        <div class="frame-pw">


            <div>
                <div class="forgot-pw-heading-top">
                    <h3 class="forgot-pw-heading">Member Log In</h3>
                </div>
                <form class="form-forgot-pw" action="" method="post" name="form">
                    <div class="img1-payment">
                        <img class="img2-payment" src="/img/Form-icon.png" alt="Snow">
                    </div>
                    <div>
                        <h3 class="heading2-forgot-pw">Forgot Your Password</h3>
                        <p class="para-forgot-pw">Enter your email id and we will send you a link to reset your password</p>

                    </div>
                    <div class="pt-4">
                        <input class="form-control form-control-lg" type="text" placeholder="Email">
                    </div>
                    <div class="pt-3">
                        <button type="button" class="btn btn-outline-primary btn1-forgot-pw">Cancel</button>
                        <button type="button" class="btn btn-outline-primary btn2-forgot-pw">Reset</button>
                    </div>
                </form>




            </div>


        </div>






    </div>
</div>

